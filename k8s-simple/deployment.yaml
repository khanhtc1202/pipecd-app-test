apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-simple
  labels:
    app: k8s-simple
spec:
  replicas: 3
  selector:
    matchLabels:
      app: k8s-simple
      pipecd.dev/variant: primary
  template:
    metadata:
      labels:
        app: k8s-simple
        pipecd.dev/variant: primary
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
      - name: helloworld
        image: ghcr.io/pipe-cd/helloworld:v0.32.0
        args:
          - server
        ports:
        - containerPort: 9085